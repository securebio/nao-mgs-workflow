name: Load Rust Container
description: Download and load the locally-built Rust tools container from artifacts
runs:
  using: composite
  steps:
    - uses: actions/download-artifact@v4
      with:
        name: rust-container
    - shell: bash
      run: gunzip -c rust-tools.tar.gz | docker load
