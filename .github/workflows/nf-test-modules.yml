name: nf-test modules test

on:
  pull_request:
    branches:
      - main
      - dev
      - stable
      - ci-test

concurrency:
  group: ci-nf-test-modules-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  # Determine which Rust container to use (ECR or locally-built)
  setup-rust:
    uses: ./.github/workflows/setup-rust-container.yml

  test-modules:
    needs: setup-rust
    runs-on:
      group: ubuntu-larger
      labels: ubuntu-16
    timeout-minutes: 15
    env:
      AWS_ACCESS_KEY_ID: ${{secrets.NAO_TESTING_ACCESS_KEY }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.NAO_TESTING_SECRET_ACCESS_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Check for relevant changes
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            modules:
              - 'tests/modules/**'
              - 'modules/**'
              - 'configs/**'
              - 'tests/configs/**'
              - '.github/workflows/nf-test-modules.yml'
              - '.github/actions/setup-nf-test/**'
              - 'rust-tools/**'
              - 'docker/nao-rust-tools.Dockerfile'
      - name: Setup nf-test environment
        if: steps.filter.outputs.modules == 'true'
        uses: ./.github/actions/setup-nf-test
        with:
          aws_access_key_id: ${{ secrets.NAO_TESTING_ACCESS_KEY }}
          aws_secret_access_key: ${{ secrets.NAO_TESTING_SECRET_ACCESS_KEY }}
          load_rust_container: ${{ needs.setup-rust.outputs.rust-changed }}
      - name: Run module tests
        if: steps.filter.outputs.modules == 'true'
        run: |
          RUST_ARGS="${{ needs.setup-rust.outputs.rust-changed == 'true' && '--profile=rust_dev' || needs.setup-rust.outputs.container-args }}"
          bin/run-nf-test.sh --num-workers 16 tests/modules --output-log test-logs-modules.txt $RUST_ARGS
      - name: Upload test results
        if: failure() && steps.filter.outputs.modules == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: test-logs-modules
          path: test-logs-modules.txt
          retention-days: 2
