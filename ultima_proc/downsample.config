/***********************************************
| CONFIGURATION FOR ILLUMINA DOWNSAMPLING     |
| Downsamples Illumina FASTQs to match Ultima |
***********************************************/

params {
    // Input: samplesheet produced by prepare_downsample.py
    samplesheet = "${projectDir}/downsample_samplesheet.csv"

    // Output: S3 directory for downsampled FASTQs
    // Update this to your desired output location
    outdir = "s3://nao-katherine/ultima-analysis/downsampled-illumina/"

    // Required by profiles.config for workDir
    base_dir = "s3://nao-katherine/ultima-analysis/downsampled-illumina"
}

// Include shared infrastructure configs from the main pipeline
includeConfig "${projectDir}/../configs/containers.config"
includeConfig "${projectDir}/../configs/resources.config"
includeConfig "${projectDir}/../configs/profiles.config"

// AWS Batch job queue — update for your environment
process.queue = "kstansifer-batch-jq"
