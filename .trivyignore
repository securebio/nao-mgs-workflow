# Trivy ignore file for known false positives and accepted risks
# See: https://aquasecurity.github.io/trivy/latest/docs/configuration/filtering/#trivyignore

# zlib/minizip: integer overflow in zipOpenNewFileInZip4_64 — Debian does not build
# the vulnerable minizip code into its zlib binary packages (Debian status: ignored)
CVE-2023-45853 exp:2026-08-31

# glibc: integer overflow in memalign — Debian bookworm marks as <no-dsa>
# (minor issue, requires attacker control of size+alignment args)
CVE-2026-0861 exp:2026-08-31

# ── Rust crates embedded in compiled Polars .so (multiqc dependency) ──
# These live inside multiqc's prebuilt _polars_runtime.abi3.so; the fix requires
# Polars to release a build against the patched crate and multiqc to pull it, so
# no yml pin can reach the fix. Re-eval when multiqc ships a Polars with the fixes.
# quinn-proto: QUIC protocol DoS
# No fix available until Polars releases a build with quinn-proto >= 0.11.14
CVE-2026-31812 exp:2026-09-30
# quinn-proto: remote memory exhaustion via unbounded out-of-order stream reassembly
# (HIGH); fixed in 0.11.15, and the shipped .so now carries 0.11.14 (i.e. the bump that
# cleared the entry above). multiqc uses Polars only for local dataframe work over
# pipeline QC files and opens no QUIC connections.
GHSA-4w2j-m93h-cj5j exp:2026-11-15
# rustls-webpki: DoS via panic on malformed CRL BIT STRING.
# No fix available until Polars releases a build with rustls-webpki >= 0.103.13.
GHSA-82j2-j2ch-gfr8 exp:2026-09-30
# pyo3: out-of-bounds read in nth/nth_back for PyList/PyTuple iterators via
# unchecked index+n overflow (HIGH); fixed in pyo3 >= 0.29.0. Reachable only if
# Polars runs those iterators over attacker-controlled n; multiqc feeds Polars its
# own pipeline QC dataframes, not untrusted iterator offsets.
GHSA-36hh-v3qg-5jq4 exp:2026-09-30

# linux-pam: directory traversal (no Debian fix available as of 2026-03-14)
CVE-2025-6020 exp:2026-08-31

# GnuPG gpgv: information disclosure / potential code execution (no Debian fix available as of 2026-03-14)
CVE-2025-68973 exp:2026-08-31

# ── Go stdlib v1.23.4 in ncbi-datasets-cli 18.21.0 (ncbi_datasets container) ──
# The NCBI datasets/dataformat binaries are pre-compiled Go executables distributed
# via conda-forge. They ship Go stdlib v1.23.4.
# Go ~= 1.25.11 or ~= 1.26.4 is required to address all vulnerabilities.
# We cannot rebuild these — only NCBI can, by updating their Go toolchain.
#
# Risk assessment: these binaries run in an isolated container that only downloads
# genome metadata from NCBI's API and writes local files. They do not serve TLS,
# connect to Postgres, or process untrusted archives — so the practical attack
# surface for these CVEs is minimal in our pipeline context.
#
# TODO: Remove these ignores when a newer ncbi-datasets-cli is available on
# conda-forge built with Go ~= 1.25.11 or ~= 1.26.4. Check with:
#   conda search -c conda-forge ncbi-datasets-cli
#   strings $(conda run -n <env> which datasets) | grep '^go1\.'
# Verified 2026-08-18: the latest conda-forge build (18.35.0) still ships Go stdlib
# go1.23.4 — identical to the pinned 18.21.0 — so bumping the pin fixes none of these.
# Confirmed by unpacking the conda package and running `strings` on datasets/dataformat.
#
# crypto/tls: unexpected session resumption (CRITICAL)
CVE-2025-68121 exp:2026-09-30
# database/sql: Postgres scan race condition (HIGH)
CVE-2025-47907 exp:2026-09-30
# archive/tar: unbounded allocation parsing GNU sparse map (HIGH)
CVE-2025-58183 exp:2026-09-30
# net/url: memory exhaustion in query parameter parsing (HIGH)
CVE-2025-61726 exp:2026-09-30
# archive/zip: excessive CPU consumption building archive index (HIGH)
CVE-2025-61728 exp:2026-09-30
# crypto/x509: DoS via crafted certificate (HIGH)
CVE-2025-61729 exp:2026-09-30
# net/url: incorrect parsing of IPv6 host literals (HIGH)
CVE-2026-25679 exp:2026-09-30
# crypto/x509: DoS via excessive processing of DNS SAN entries in a crafted
# certificate (HIGH); these binaries validate only NCBI/AWS server certs.
CVE-2026-27145 exp:2026-09-30
# crypto/x509: excessive CPU in certificate chain building (HIGH)
CVE-2026-32280 exp:2026-09-30
# internal/syscall/unix: Root.Chmod can follow symlinks out of the root (HIGH)
CVE-2026-32282 exp:2026-09-30
# crypto/x509: DoS via inefficient certificate chain validation (HIGH)
CVE-2026-32281 exp:2026-09-30
# crypto/tls: TLS 1.3 post-handshake key-update deadlock DoS (HIGH)
CVE-2026-32283 exp:2026-09-30
# net (cgo resolver): LookupCNAME mishandling of overlong CNAME responses (HIGH)
CVE-2026-33811 exp:2026-09-30
# net/http: HTTP/2 transport infinite loop on crafted SETTINGS frames (HIGH)
CVE-2026-33814 exp:2026-09-30
# net/mail: DoS via crafted ParseAddress/ParseAddressList/ParseDate inputs (HIGH)
CVE-2026-39820 exp:2026-09-30
# net: panic in Dial/LookupPort when handling NUL byte (HIGH)
CVE-2026-39836 exp:2026-09-30
# net/mail: DoS via pathological consumePhrase inputs (HIGH)
CVE-2026-42499 exp:2026-09-30
# html/template: XSS via meta-content URL escaping bypass (HIGH); server-side
# templating, never used by these client-only metadata-download binaries.
CVE-2026-39823 exp:2026-09-30
# net/http/httputil: ReverseProxy query smuggling past urlmaxqueryparams (HIGH);
# these binaries are HTTP clients, run no reverse proxy.
CVE-2026-39825 exp:2026-09-30
# html/template: XSS via script type-attribute escaper bypass (HIGH); as above,
# no server-side templating in these CLI tools.
CVE-2026-39826 exp:2026-09-30
# mime/net-mail: excessive CPU decoding crafted MIME headers with many invalid
# encoded-words (HIGH); these CLIs fetch JSON from NCBI's API, never parse email/MIME.
CVE-2026-42504 exp:2026-09-30
# Eight further Go stdlib CVEs surfaced 2026-08-18; same binaries, same assessment, and
# the upstream check above shows no newer build to move to.
# encoding/asn1: DoS via excessive recursion in Unmarshal (HIGH); ASN.1 parsing here is
# limited to NCBI/AWS server certificates.
CVE-2026-33818 exp:2026-11-15
# golang.org/x/net/idna: privilege escalation via incorrect Punycode label processing
# (HIGH); hostnames are the hardcoded NCBI/AWS endpoints, not attacker-supplied.
CVE-2026-39821 exp:2026-11-15
# os.Root: symlink following allows directory traversal (HIGH); these binaries write to
# the workflow's own task directory, with no untrusted symlink sources.
CVE-2026-39822 exp:2026-11-15
# net/http: unencrypted HTTP/2 connections vulnerable to DoS (HIGH); all traffic is
# client-side HTTPS to NCBI/AWS.
CVE-2026-56853 exp:2026-11-15
# html/template: XSS via pathological input (HIGH); no server-side templating in these
# client-only CLI tools.
CVE-2026-56858 exp:2026-11-15
# encoding/xml: DoS via XML decoding recursion depth (HIGH); these CLIs consume JSON
# from NCBI's API, not XML.
CVE-2026-56859 exp:2026-11-15
# net/url: DoS from quadratic complexity in path resolution (HIGH); URLs are constructed
# by the tools themselves against known endpoints.
CVE-2026-56860 exp:2026-11-15
# crypto/tls: DoS via indefinite KeyUpdate messages (HIGH); requires a hostile TLS peer,
# and these binaries talk only to NCBI/AWS.
CVE-2026-56862 exp:2026-11-15

# ── Pillow in multiqc container ──
# (CVE-2026-40192 and CVE-2026-42311 were dropped 2026-08-18: both are fixed as of
# Pillow 12.2.0, which the container already ships, so the entries were stale. The
# claim they carried — that Pillow cannot be pinned independently — is also wrong;
# containers/multiqc.yml now pins it explicitly, the same way it pins urllib3.)

# ── Debian trixie base image (mambaorg/micromamba): no fixes reachable ──
# The base-image knob (container-base-image in pyproject.toml) is the only
# permitted lever for these system packages, and the current latest micromamba
# digest still ships the vulnerable versions — the fixes are not yet installed
# in any published base layer. Re-eval when micromamba rebuilds against a trixie
# apt state carrying the fix (then bump the digest and drop the entry).
#
# glibc: DoS via iconv() with specific character set conversions (fix_deferred)
# Our containers don't perform attacker-controlled iconv conversions.
CVE-2026-4046 exp:2026-08-31

# ncurses: stack-based buffer overflow in infocmp CLI tool (no-dsa, low urgency)
# Our containers never invoke infocmp. Fix only in sid (6.6+).
CVE-2025-69720 exp:2026-08-31

# openssl (libssl3t64, openssl-provider-legacy): unbounded memory growth in the QUIC
# server path -- Initial packets for unknown connection IDs allocate channel objects
# that queue unbounded until SSL_accept() drains them (HIGH). Debian marks it postponed
# ("minor issue, fix along with future update") and deb13u2 is still vulnerable, so no
# fix is reachable. Our containers run no QUIC or TLS server of any kind; openssl is
# linked only for outbound TLS to known NCBI/AWS endpoints via curl/python.
# Re-eval: Debian ships a fixed openssl for trixie.
CVE-2026-14456 exp:2026-10-31

# acl (libacl1) / attr (libattr1): symlink-traversal local privilege escalation
# in the acl_*_file() functions and getfattr/setfattr utilities (HIGH). Fixed only
# in Debian sid (acl 2.4.0-1, attr 1:2.6.0-1); trixie awaits a point release, so
# no base-image digest carries the fix yet. The pipeline never invokes getfattr/
# setfattr or the pathname-based libacl APIs, and containers run as root — outside
# the non-root local-attacker threat model. Re-eval: trixie point release / a
# micromamba rebuild that carries it.
CVE-2026-54369 exp:2026-08-31
CVE-2026-54371 exp:2026-08-31

# systemd: IPC API mishandling from unprivileged callers causing service freeze
# Our containers don't run systemd services. Fix only in sid (260.1+).
CVE-2026-29111 exp:2026-08-31

# libgcrypt20: heap overflow / DoS via crafted ECDH ciphertext to gcry_pk_decrypt
# (no Debian fix available as of 2026-04-29; fixed in upstream libgcrypt >= 1.12.2)
# Our pipeline containers never decrypt attacker-controlled ECDH ciphertext.
# Check for a Debian fix at:
#   https://security-tracker.debian.org/tracker/CVE-2026-41989
CVE-2026-41989 exp:2026-08-31

# libcap2: setcap (cap_set_file) exploit usable by non-root users.
# (No Debian fix available as of 2026-05-01.) Our pipeline containers do
# not invoke setcap/cap_set_file, so we do not call the affected code;
# additionally, they run as root, which puts them outside the non-root
# threat model of this CVE.
# Check for a Debian fix at:
#   https://security-tracker.debian.org/tracker/CVE-2026-4878
CVE-2026-4878 exp:2026-08-31

# ── urllib3 2.6.3 transitive via awscli=2.34.9 in blast, bowtie2_samtools,
# kraken2, minimap2_samtools ──
# Our awscli usage is `update_blastdb.pl --source aws` and equivalents — HTTPS
# to known NCBI/AWS endpoints, no proxy, no attacker-controlled compressed
# streams; neither CVE is reachable. Fix exists in urllib3>=2.7.0 but every
# current conda-forge awscli build pins urllib3<=2.6.3; re-eval when the
# feedstock relaxes the cap.
# CVE-2026-44431 (HIGH): cross-origin header leak in proxied low-level redirects.
CVE-2026-44431 exp:2026-09-30
# CVE-2026-44432 (HIGH): decompression-bomb safeguard bypass in streaming reads.
CVE-2026-44432 exp:2026-09-30

# ── perl-base (Debian trixie 5.40.1-6): no fixed version available as of 2026-06-04 ──
# The pipeline's only perl invocation is `update_blastdb.pl` in the maintainer-run
# INDEX step (modules/local/downloadBlastDB), which fetches official NCBI/AWS BLAST
# databases via `--source aws`. perl-base is otherwise present only as a Debian
# base-image dependency, not called by any pipeline tool. None of these CVEs is
# reachable on attacker-controlled input in that context. Re-eval: Debian backports
# a fix to trixie perl-base (a rebuild then clears them), or the pipeline gains a
# perl path that processes untrusted tar/regex/glob input.
# Archive::Tar symlink path traversal on extraction (CRITICAL); BLAST DB tarballs are trusted.
CVE-2026-42496 exp:2026-08-31
# perl regex compiler (Perl_study_chunk) heap overflow (CRITICAL); no attacker-controlled regex is compiled.
CVE-2026-8376 exp:2026-08-31
# Archive::Tar hardlink path traversal on extraction (HIGH); same trusted-tarball-only exposure as CVE-2026-42496.
CVE-2026-42497 exp:2026-08-31
# IO::Compress/File::GlobMapper eval-string injection via output glob (HIGH); no attacker-controlled glob.
CVE-2026-48962 exp:2026-08-31
# Archive::Tar unbounded allocation -> memory-exhaustion DoS (HIGH); trusted tarballs, worst case ephemeral OOM.
CVE-2026-9538 exp:2026-08-31
# Three further perl CVEs surfaced 2026-08-18. trixie is still on 5.40.1-6 and the
# latest published micromamba digest ships that same version, so no fix is reachable
# via the permitted base-image bump. Same exposure as above: the only perl invocation
# is update_blastdb.pl against official NCBI/AWS BLAST databases.
# Perl_study_chunk 16-bit overflow silently miscompiles regexes with >65535 fixed-string
# alternation branches (CRITICAL); no pipeline regex is attacker-supplied or that large.
CVE-2026-13221 exp:2026-10-31
# pack/unpack S_measure_struct integer overflow -> OOB heap read (HIGH); the pipeline
# builds no pack templates from untrusted input.
CVE-2026-57432 exp:2026-10-31
# Storable retrieve_hook_common signed overflow aborts thaw()/retrieve() on a crafted
# SX_HOOK blob (CRITICAL); the pipeline deserializes no Storable data at all.
CVE-2026-57433 exp:2026-10-31

# ── gzip (Debian trixie 1.13-1): no fixed version available as of 2026-07-02 ──
# gzip: global buffer overflow in the LZH (.lzh) decompressor on a crafted archive
# (HIGH, CVE published 2026-06-29). gzip is a base-layer package and the container
# Dockerfiles install only `procps` (no `apt upgrade`), so the fix is not reachable
# via the permitted base-image bump; the pipeline also never decompresses attacker-
# controlled .lzh archives. Re-eval when Debian trixie ships a fixed gzip (a base-
# image rebuild then clears it).
CVE-2026-41992 exp:2026-08-31

# ── util-linux (Debian trixie 2.41-5): fix exists but is not reachable ──
# libblkid integer overflow in the DOS partition-table parser (libblkid/src/partitions/
# dos.c) reached by probing a crafted disk image or partition table (HIGH). Debian fixed
# it in 2.41.5-0+deb13u1, but the latest published micromamba digest still ships 2.41-5
# and the container Dockerfiles install only `procps` (no `apt upgrade`), so the fix is
# not reachable via the permitted base-image bump. The pipeline never probes block
# devices or parses disk images: util-linux/libblkid are present only as base-layer
# dependencies of mount/login, which nothing in the workflow invokes.
# Re-eval: a micromamba rebuild carrying 2.41.5-0+deb13u1 (bump the digest and drop this).
CVE-2026-53615 exp:2026-10-31

# Same util-linux 2.41-5, same unreachable fix (2.41.5-0+deb13u1): three mount(8) flaws.
# Two TOCTOU races in mount applying post-mount ownership/mode changes and in resolving
# the target path via an ancestor-directory swap, plus a nosuid/noexec bypass through
# LIBMOUNT_FORCE_MOUNT2 (all HIGH). All three require executing mount(8); the pipeline
# never does, and Nextflow/Fusion perform their mounts on the host outside the container.
# mount is SUID root in the base image, so these remain a local privilege-escalation
# surface for an attacker who already has code execution inside a container - defence in
# depth rather than unreachable, which is why the expiry is short.
# Re-eval: a micromamba rebuild carrying 2.41.5-0+deb13u1 (bump the digest and drop these).
CVE-2026-53612 exp:2026-10-31
CVE-2026-53613 exp:2026-10-31
CVE-2026-53614 exp:2026-10-31
