// The production error strategy (retry, then ignore), which nf_test otherwise replaces with 'finish'
includeConfig "run.config"

profiles {
    nf_test {
        process.errorStrategy = { task.attempt <= task.maxRetries ? 'retry' : 'ignore' }
    }
}
