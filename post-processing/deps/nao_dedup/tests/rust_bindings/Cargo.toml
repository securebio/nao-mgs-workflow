[package]
name = "nao_dedup_python"
version = "0.1.0"
edition = "2021"

[lib]
name = "nao_dedup_rust"
crate-type = ["cdylib"]

[dependencies]
nao_dedup = { path = "../.." }
pyo3 = { version = "0.22", features = ["extension-module"] }
